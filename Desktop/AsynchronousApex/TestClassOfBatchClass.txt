@isTest
public class TestBatchclassOfLead {
    @isTest
    public static void LeadTest(){
        
        List<Lead> leadList = new List<Lead>();
        for(Integer i=0;i<3;i++){
            lead l = new Lead();
            l.FirstName= 'Sarjerao';
            l.LastName = 'Pujari';
            l.Company = 'Decipher';
            l.Status = 'Closed - Converted';
            leadList.add(l);
            
            
        }
        insert leadList;
        Test.startTest();
        RemoveDuplicateLeads ld = new RemoveDuplicateLeads();
        Database.executeBatch(ld);
        Test.stopTest();
        
        List<Lead> leads = [select Name, Company, Status from lead ];
        system.assertEquals(1,leads.size());
    }
    @isTest
    public static void testschedule() {
        Test.StartTest();
        RemoveDuplicateLeads testsche = new RemoveDuplicateLeads();
        String sch = '0 0 0/2 ? * * *';
        system.schedule('Test status Check', sch, testsche );
        Test.stopTest();
    }
}